---
const base = import.meta.env.BASE_URL;
---
<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<title>CLOZE</title>
		<script is:inline define:vars={{ base }}>
			const lang = (navigator.language || navigator.userLanguage || '').toLowerCase().startsWith('ja') ? 'ja' : 'en';
			window.location.replace(base + lang + '/');
		</script>
		<noscript>
			<meta http-equiv="refresh" content={`0;url=${base}en/`} />
		</noscript>
		<style is:global>
			body { font-family: system-ui, sans-serif; padding: 2rem; text-align: center; }
			a { color: #3245ff; }
		</style>
	</head>
	<body>
		<p>Redirecting… <a href={`${base}en/`}>Go to CLOZE</a></p>
	</body>
</html>
